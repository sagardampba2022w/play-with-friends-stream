For backend development, use `uv` for dependency management. a few useful commands are listed below:

Useful Commands

    # Sync dependencies from lockfile
    uv sync

    # Add a new package
    uv add <PACKAGE-NAME>

    # Run Python files
    uv run python <PYTHON-FILE>

    # Regularly commit code to git
  
